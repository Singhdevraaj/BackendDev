# Learning of NodeJs and Backend Development
- app.use() can be also use to create route.
- The order of all the route matters because if an route string is like "/" and on top then all the other routes start with the same string like "/hello","/test","/signup" ,etc give the route handler of that first route with string "/".